import { RecallRecognitionExperimentExecution } from './recall-recognition-experiment-execution';

describe('RecallRecognitionExperimentExecution', () => {
  it('should create an instance', () => {
    expect(new RecallRecognitionExperimentExecution()).toBeTruthy();
  });
});
