import { ExperimentFeedback } from './experiment-feedback';

describe('ExperimentFeedback', () => {
  it('should create an instance', () => {
    expect(new ExperimentFeedback()).toBeTruthy();
  });
});
