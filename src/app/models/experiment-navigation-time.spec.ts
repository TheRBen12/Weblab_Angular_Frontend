import { ExperimentNavigationTime } from './experiment-navigation-time';

describe('ExperimentNavigationTime', () => {
  it('should create an instance', () => {
    expect(new ExperimentNavigationTime()).toBeTruthy();
  });
});
