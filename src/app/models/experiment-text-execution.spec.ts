import { ExperimentTestExecution } from './experiment-test-execution';
import {
  ExperimentTestExecutionComponent
} from '../experiment-tests/experiment-test-execution/experiment-test-execution.component';

describe('ExperimentTextExecution', () => {
  it('should create an instance', () => {
    expect(new ExperimentTestExecutionComponent()).toBeTruthy();
  });
});
