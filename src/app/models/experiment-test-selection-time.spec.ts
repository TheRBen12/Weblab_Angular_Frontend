import { ExperimentTestSelectionTime } from './experiment-test-selection-time';

describe('ExperimentTestSelectionTime', () => {
  it('should create an instance', () => {
    expect(new ExperimentTestSelectionTime()).toBeTruthy();
  });
});
