import { NavigationClickTime } from './navigation-click-time';

describe('NavigationClickTime', () => {
  it('should create an instance', () => {
    expect(new NavigationClickTime()).toBeTruthy();
  });
});
