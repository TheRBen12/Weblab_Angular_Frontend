import { ExperimentSelectionTime } from './experiment-selection-time';

describe('ExperimentSelectionTime', () => {
  it('should create an instance', () => {
    expect(new ExperimentSelectionTime()).toBeTruthy();
  });
});
