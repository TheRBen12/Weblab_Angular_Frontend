import { MentalModelShopConfiguration } from './mental-model-shop-configuration';

describe('MentalModelShopConfiguration', () => {
  it('should create an instance', () => {
    expect(new MentalModelShopConfiguration()).toBeTruthy();
  });
});
